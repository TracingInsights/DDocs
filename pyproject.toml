[project]
name = "fia-f1-documents"
version = "0.1.0"
description = "Scrape and extract FIA Formula 1 documents."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aiofiles>=25.1.0",
    "aiohttp>=3.14.3",
    "beautifulsoup4>=4.15.0",
    "curl-cffi>=0.16.3",
    "pdfplumber>=0.11.10,<0.11.11",
    "kreuzberg>=4.10.3",
    "pymupdf>=1.28.2",
    "jinja2>=3.1.6",
    "marker-pdf>=2.0.0,<3",
    "pypdf>=6.19.0",
    "rich>=15.0.0",
    "duckdb>=1.5.5",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.8",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
exclude = [
    "/.venv",
    "/.github",
    "/classification",
    "/documents",
    "/extracted",
    "/processed_documents",
    "/scratch",
    "*.md",
    "*.json",
    "test_*.py",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 120
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.uv]
environments = ["sys_platform == 'linux'"]
